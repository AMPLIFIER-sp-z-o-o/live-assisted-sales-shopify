{
  "name": "amper-las-shopify",
  "private": true,
  "scripts": {
    "dev": "shopify app dev",
    "deploy": "shopify app deploy",
    "info": "shopify app info"
  },
  "devDependencies": {
    "@shopify/cli": "^3.80.0"
  }
}
